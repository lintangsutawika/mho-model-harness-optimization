# MHO Training Setup Status — 2026-09-07

## Goal
Run SkyRL DAPO math training on `abci` HPC using singularity, with harbor sandboxes
running EXTERNAL to the training container (Modal-like but local).

## Architecture (current)
```
Training (in SIF):  HarborGenerator -> executor_client -> HTTP -> 127.0.0.1:8900
Executor (host):    executor_service.py -> harbor.Trial -> singularity sandbox (writable)
```
- Training runs in `skyrl-train-ray-2.57.0-py3.12-cu13.0-megatron.sif` (present, 7.4G).
- Sandboxes run on the same node via host singularity, outside the training container.
- No nested singularity (per user), no Docker (none on HPC), no Modal (chose local external).

## Files (all under /home/aci18914wh/mho-model-harness-optimization/mho-model-harness-optimization/)
- `scripts/executor/executor_service.py`   NEW — external FastAPI executor (runs harbor Trial on host, writable-sandbox singularity)
- `scripts/executor/executor_client.py`    NEW — HTTP client for HarborGenerator
- `src/harbor/generator.py`   PATCHED — routes trials through executor when EXECUTOR_URL set
- `src/train_entrypoint.py`   PATCHED — HARBOR_ENV_TYPE override
- `scripts/train/train_math_dapo.sh`  PATCHED — stripped nested block; PYTHONPATH+=scripts/executor; EXECUTOR_URL/HARBOR_ENV_TYPE
- `.env`  — EXECUTOR_URL=http://127.0.0.1:8900, HARBOR_ENV_TYPE=singularity, BASE_SIF set
- Host executor venv: /home/aci18914wh/executor_env (harbor@c178c20 + fastapi + uvicorn + httpx)

## Verified
- SIF boots via singularity; uv env built (373 pkgs), skyrl+megatron import OK.
- Executor service imports, starts (uvicorn), /health OK.
- Client round-trip: executor_enabled()=True, health OK (HTTP works).
- Host singularity writable-sandbox (build --sandbox + --writable) proven.
- All patched files compile; train_math_dapo.sh syntax OK.

## BLOCKER — RESOLVED
fast-hadamard-transform build failure is FIXED.
  - Commented out FAST_HADAMARD_TRANSFORM_FORCE_BUILD=TRUE in [tool.uv.extra-build-variables]
    of pyproject.toml (backup: /home/aci18914wh/pyproject.toml.bak).
  - uv now resolves the prebuilt cp312 wheel (fast_hadamard_transform-1.1.0+cu13torch2.11cxx11abitrue)
    for our platform (cp312 x86_64 linux) per uv.lock resolution markers.
  - VERIFIED: `uv run --isolated --extra megatron` builds 373 pkgs, fast_hadamard_transform
    imports (BUILD_OK). skyrl + torch import too.

## Remaining (verify)
- Run train_math_dapo.sh inside PBS alloc (job 2239802 / hnode074) -> first end-to-end trial
  (sandbox -> agent -> vLLM at localhost via external executor).

## Launch (once build fixed)
# step 1: start executor (from repo root, inside PBS alloc)
cd /home/aci18914wh/mho-model-harness-optimization/mho-model-harness-optimization
PYTHONPATH="$PWD/src:$PWD/scripts/executor" EXECUTOR_PORT=8900 \
  nohup /home/aci18914wh/executor_env/.venv/bin/python -m executor_service \
  > /home/aci18914wh/executor_svc.log 2>&1 &
# confirm: curl -s http://127.0.0.1:8900/health  -> {"status":"ok"}
# step 2: train
bash scripts/train/train_math_dapo.sh